# Linear Vacations Agent

This agent provides a set of Bash helpers for managing team vacation data in Linear.

## Overview
- **Label**: `Vacation` (id: `d170d689-0a00-40d8-946c-80cdcd41bdc4`)
- **Project**: `Team Vacations` (id: `8501ab78-a3b8-49b1-9506-2da8123c7809`)
- **Initiative**: `Team Management` (id: `e12a0244-b4e4-4feb-8dbd-eefe5a96410f`)

## Tools
The repo includes a single script placed under `src/tools/linear_vacations.sh` that implements the following functions:

```bash
add_vacation   <name> <start> <end> [note]
update_vacation <issue_id> <start> <end> [note]
delete_vacation <issue_id>
list_vacations
```

All functions use Linear's GraphQL API via `curl` and expect the environment variable `LINEAR_API_KEY` to be set.

## Usage
```bash
source src/tools/linear_vacations.sh
# Add a vacation
add_vacation "Ana Silva" "2026-07-01" "2026-07-10" "Family trip"
# List all vacations
list_vacations
```

## Integration
- The script works out‑of‑the‑box with the *Team Vacations* project and the *Vacation* label created by the initial setup.
- The weekly digest script (`linear_vacation_digest.sh`) lives in your home `~/bin/` folder and posts a project update each Monday.

## Extending
- Add more helper functions (e.g., bulk import, CSV export) by copying the pattern in `linear_vacations.sh`.
- For agents that need to call these helpers, simply source the script in their runtime environment.

---
*Generated by Axemaster – your chief‑of‑staff automation engine.*
