# Axeng — Python dependencies
# Install with: pip install -r requirements.txt
# Or for full features: pip install axeng[full]
#
# System dependencies:
# - GitHub CLI (gh): brew install gh
# - Node.js 18+: brew install node
# - Python 3.12+: brew install python@3.12

# Core CLI (all you need for axeng standalone)
pyyaml>=6.0
typer>=0.9.0
rich>=13.0.0
requests>=2.32.0
jinja2>=3.1.0

# API server (for Next.js UI / web)
fastapi>=0.110.0
uvicorn>=0.28.0
pydantic>=2.0.0

# LLM providers
anthropic>=0.20.0
openai>=1.30.0

# Google Workspace
google-api-python-client>=2.120.0
google-auth>=2.30.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0