# Axeng — Homebrew Dependencies
# Install with: brew bundle

# Core dependencies
brew "python@3.12"
brew "gh"              # GitHub CLI (for gh auth login)
brew "git"

# Container runtime
cask "docker"          # Docker Desktop for Mac

# Optional but recommended
brew "make"            # GNU Make for Makefile commands
